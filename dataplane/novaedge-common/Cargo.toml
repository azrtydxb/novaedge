[package]
name = "novaedge-common"
version = "0.1.0"
edition = "2021"

[features]
default = []
userspace = ["aya"]

[target.'cfg(target_os = "linux")'.dependencies]
aya = { version = "0.13", optional = true }
