[extend]
# Use the default gitleaks config as the base
useDefault = true

[allowlist]
description = "Allow sample/example Kubernetes manifests, documentation, and test fixtures"
paths = [
  '''config/samples/.*\.yaml''',
  '''config/samples/.*\.yml''',
  '''docs/.*\.md''',
  '''.*_test\.go''',
]
