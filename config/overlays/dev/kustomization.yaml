apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nova-system

# Base configuration
resources:
- ../../default

# Development patches
patches:
# Reduce controller replicas for dev
- patch: |-
    - op: replace
      path: /spec/replicas
      value: 1
  target:
    kind: Deployment
    name: novaedge-controller

# Reduce resource limits for dev
- patch: |-
    - op: replace
      path: /spec/template/spec/containers/0/resources/limits/cpu
      value: 200m
    - op: replace
      path: /spec/template/spec/containers/0/resources/limits/memory
      value: 256Mi
    - op: replace
      path: /spec/template/spec/containers/0/resources/requests/cpu
      value: 50m
    - op: replace
      path: /spec/template/spec/containers/0/resources/requests/memory
      value: 64Mi
  target:
    kind: Deployment
    name: novaedge-controller

# Remove node selector from agent for dev (run on all nodes)
- patch: |-
    - op: remove
      path: /spec/template/spec/nodeSelector
  target:
    kind: DaemonSet
    name: novaedge-agent

# Set imagePullPolicy to Never for local images (controller)
- patch: |-
    - op: replace
      path: /spec/template/spec/containers/0/imagePullPolicy
      value: Never
  target:
    kind: Deployment
    name: novaedge-controller

# Set imagePullPolicy to Never for local images (webui)
- patch: |-
    - op: replace
      path: /spec/template/spec/containers/0/imagePullPolicy
      value: Never
  target:
    kind: Deployment
    name: novaedge-webui

# Set imagePullPolicy to Never for local images (agent)
- patch: |-
    - op: replace
      path: /spec/template/spec/containers/0/imagePullPolicy
      value: Never
  target:
    kind: DaemonSet
    name: novaedge-agent

# Image configuration for dev (use dev tag to avoid latest auto-pull behavior)
images:
- name: ghcr.io/azrtydxb/novaedge-controller
  newName: novaedge-controller
  newTag: dev
- name: ghcr.io/azrtydxb/novaedge-agent
  newName: novaedge-agent
  newTag: dev
- name: ghcr.io/azrtydxb/novaedge-novactl
  newName: novaedge-novactl
  newTag: dev

# ConfigMap for development settings
configMapGenerator:
- name: novaedge-config
  literals:
  - LOG_LEVEL=debug
  - METRICS_ENABLED=true
