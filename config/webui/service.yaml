apiVersion: v1
kind: Service
metadata:
  name: novaedge-webui
  namespace: nova-system
  labels:
    app.kubernetes.io/name: novaedge-webui
    app.kubernetes.io/component: webui
    app.kubernetes.io/part-of: novaedge
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app.kubernetes.io/name: novaedge-webui
