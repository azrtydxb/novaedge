{{- if .Values.metrics.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "novaedge-operator.fullname" . }}-metrics
  labels:
    {{- include "novaedge-operator.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "novaedge-operator.selectorLabels" . | nindent 4 }}
{{- end }}
