---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: novaedgeremoteclusters.novaedge.io
spec:
  group: novaedge.io
  names:
    kind: NovaEdgeRemoteCluster
    listKind: NovaEdgeRemoteClusterList
    plural: novaedgeremoteclusters
    shortNames:
    - nerc
    singular: novaedgeremotecluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - jsonPath: .spec.region
      name: Region
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.connection.connected
      name: Connected
      type: boolean
    - jsonPath: .status.agents.ready
      name: Agents
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NovaEdgeRemoteCluster is the Schema for the novaedgeremoteclusters API
          It represents a remote/edge cluster in a hub-spoke multi-cluster deployment
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              NovaEdgeRemoteClusterSpec defines the desired state for remote cluster configuration.
              It represents a remote/edge cluster that runs agents
              connecting back to the hub cluster's controller
            properties:
              agent:
                description: Agent defines the configuration for agents in this remote
                  cluster
                properties:
                  extraAnnotations:
                    additionalProperties:
                      type: string
                    description: ExtraAnnotations are additional annotations for agent
                      pods
                    type: object
                  extraArgs:
                    description: ExtraArgs are additional arguments passed to agents
                    items:
                      type: string
                    type: array
                  extraEnv:
                    description: ExtraEnv are additional environment variables for
                      agents
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  extraLabels:
                    additionalProperties:
                      type: string
                    description: ExtraLabels are additional labels for agent pods
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: NodeSelector defines which nodes should run agents
                      in the remote cluster
                    type: object
                  resources:
                    description: Resources defines resource requirements for agents
                      in this cluster
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  tolerations:
                    description: Tolerations for agent pods in the remote cluster
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists and Equal. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  version:
                    description: |-
                      Version overrides the agent version for this cluster
                      If not set, uses the hub cluster's default version
                    type: string
                  vip:
                    description: VIP defines VIP configuration for this remote cluster
                    properties:
                      bgp:
                        description: BGP defines the BGP configuration for VIP announcement
                        properties:
                          asn:
                            description: ASN is the local autonomous system number
                            format: int32
                            maximum: 4294967295
                            minimum: 1
                            type: integer
                          peers:
                            description: Peers defines the list of BGP peer configurations
                            items:
                              description: ClusterBGPPeer defines a BGP peer configuration
                                for NovaEdgeCluster
                              properties:
                                address:
                                  description: Address is the IP address of the BGP
                                    peer
                                  type: string
                                asn:
                                  description: ASN is the peer's autonomous system
                                    number
                                  format: int32
                                  maximum: 4294967295
                                  minimum: 1
                                  type: integer
                                passwordSecretRef:
                                  description: Password is the BGP session password
                                    (optional)
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                port:
                                  default: 179
                                  description: Port is the BGP port (default 179)
                                  format: int32
                                  type: integer
                              required:
                              - address
                              - asn
                              type: object
                            type: array
                          routerID:
                            description: RouterID is the BGP router ID
                            type: string
                        required:
                        - asn
                        type: object
                      enabled:
                        default: true
                        description: Enabled enables VIP management
                        type: boolean
                      interface:
                        description: Interface is the network interface for VIP binding
                          (L2 mode)
                        type: string
                      mode:
                        default: L2
                        description: Mode is the VIP mode (L2, BGP, or OSPF)
                        enum:
                        - L2
                        - BGP
                        - OSPF
                        type: string
                    type: object
                type: object
              clusterName:
                description: ClusterName is a unique identifier for this remote cluster
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              connection:
                description: Connection defines how agents in this cluster connect
                  to the hub controller
                properties:
                  controllerEndpoint:
                    description: |-
                      ControllerEndpoint is the address of the hub controller's gRPC endpoint
                      For Direct mode: "controller.nova-system.svc.cluster.local:9090" or external address
                      For Tunnel mode: tunnel relay address
                    type: string
                  mode:
                    default: Direct
                    description: |-
                      Mode is the connection mode
                      - Direct: Agents connect directly to the hub controller (requires network connectivity)
                      - Tunnel: Agents connect through a tunnel/relay (for NAT/firewall traversal)
                    enum:
                    - Direct
                    - Tunnel
                    type: string
                  reconnectInterval:
                    default: 30s
                    description: ReconnectInterval is how often to attempt reconnection
                      if disconnected
                    type: string
                  timeout:
                    default: 10s
                    description: Timeout is the connection timeout
                    type: string
                  tls:
                    description: TLS defines the mTLS configuration for secure communication
                    properties:
                      caSecretRef:
                        description: |-
                          CASecretRef references a secret containing the CA certificate for validating the controller
                          The secret must be created in the remote cluster
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      clientCertSecretRef:
                        description: |-
                          ClientCertSecretRef references a secret containing the client certificate and key
                          This secret is used by agents to authenticate to the controller
                        properties:
                          name:
                            description: name is unique within a namespace to reference
                              a secret resource.
                            type: string
                          namespace:
                            description: namespace defines the space within which
                              the secret name must be unique.
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      enabled:
                        default: true
                        description: Enabled enables mTLS for agent-controller communication
                        type: boolean
                      insecureSkipVerify:
                        default: false
                        description: InsecureSkipVerify skips TLS certificate verification
                          (NOT recommended for production)
                        type: boolean
                      serverName:
                        description: ServerName is the expected server name for TLS
                          verification
                        type: string
                    type: object
                  tunnel:
                    description: Tunnel defines tunnel-specific configuration (when
                      Mode=Tunnel)
                    properties:
                      relayEndpoint:
                        description: RelayEndpoint is the address of the tunnel relay
                          server
                        type: string
                      type:
                        default: WireGuard
                        description: Type is the tunnel type
                        enum:
                        - WireGuard
                        - SSH
                        - WebSocket
                        type: string
                      wireGuard:
                        description: WireGuard defines WireGuard-specific configuration
                        properties:
                          allowedIPs:
                            description: AllowedIPs are the allowed IP ranges for
                              the tunnel
                            items:
                              type: string
                            type: array
                          endpoint:
                            description: Endpoint is the hub's WireGuard endpoint
                            type: string
                          listenPort:
                            description: ListenPort is the local UDP port for the
                              WireGuard interface.
                            format: int32
                            type: integer
                          persistentKeepalive:
                            default: 25
                            description: PersistentKeepalive is the keepalive interval
                              in seconds
                            format: int32
                            type: integer
                          privateKeySecretRef:
                            description: PrivateKeySecretRef references a secret containing
                              the WireGuard private key
                            properties:
                              key:
                                description: The key of the secret to select from.  Must
                                  be a valid secret key.
                                type: string
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              optional:
                                description: Specify whether the Secret or its key
                                  must be defined
                                type: boolean
                            required:
                            - key
                            type: object
                            x-kubernetes-map-type: atomic
                          publicKey:
                            description: PublicKey is the hub's WireGuard public key
                            type: string
                        type: object
                    type: object
                required:
                - controllerEndpoint
                type: object
              healthCheck:
                description: HealthCheck defines how the hub monitors this remote
                  cluster
                properties:
                  enabled:
                    default: true
                    description: Enabled enables health checking of the remote cluster
                    type: boolean
                  failoverEnabled:
                    default: true
                    description: FailoverEnabled enables automatic failover when cluster
                      becomes unhealthy
                    type: boolean
                  healthyThreshold:
                    default: 2
                    description: HealthyThreshold is the number of consecutive successes
                      required
                    format: int32
                    minimum: 1
                    type: integer
                  interval:
                    default: 30s
                    description: Interval is the health check interval
                    type: string
                  timeout:
                    default: 10s
                    description: Timeout is the health check timeout
                    type: string
                  unhealthyThreshold:
                    default: 3
                    description: UnhealthyThreshold is the number of consecutive failures
                      required
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Labels are additional labels to apply to resources from
                  this cluster
                type: object
              overlayCIDR:
                description: |-
                  OverlayCIDR is the overlay network CIDR assigned to this remote cluster
                  for site-to-site routing (e.g., "10.200.1.0/24").
                type: string
              paused:
                default: false
                description: Paused suspends reconciliation for this remote cluster
                type: boolean
              region:
                description: Region is the geographic region of the remote cluster
                type: string
              routing:
                description: Routing defines how traffic should be routed to/from
                  this cluster
                properties:
                  allowCrossClusterTraffic:
                    default: true
                    description: AllowCrossClusterTraffic allows traffic to be routed
                      across clusters
                    type: boolean
                  enabled:
                    default: true
                    description: Enabled enables routing of traffic to/from this cluster
                    type: boolean
                  endpoints:
                    description: Endpoints restricts which endpoints from this cluster
                      should be included
                    properties:
                      excludeNamespaces:
                        description: ExcludeNamespaces excludes endpoints from specific
                          namespaces
                        items:
                          type: string
                        type: array
                      matchExpressions:
                        description: MatchExpressions selects endpoints matching expressions
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels selects endpoints with matching labels
                        type: object
                      namespaces:
                        description: Namespaces restricts endpoints to specific namespaces
                        items:
                          type: string
                        type: array
                    type: object
                  localPreference:
                    default: true
                    description: LocalPreference enables preferring local backends
                      within the cluster
                    type: boolean
                  priority:
                    default: 100
                    description: |-
                      Priority is the routing priority (lower = higher priority)
                      Used for failover between clusters
                    format: int32
                    maximum: 1000
                    minimum: 1
                    type: integer
                  weight:
                    default: 100
                    description: Weight is the traffic weight for weighted routing
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                type: object
              zone:
                description: Zone is the availability zone of the remote cluster
                type: string
            required:
            - clusterName
            - connection
            type: object
          status:
            description: NovaEdgeRemoteClusterStatus defines the observed state of
              NovaEdgeRemoteCluster
            properties:
              agents:
                description: Agents is the status of agents in this remote cluster
                properties:
                  healthy:
                    description: Healthy is the number of healthy agents
                    format: int32
                    type: integer
                  nodes:
                    description: Nodes lists the nodes with agent status
                    items:
                      description: RemoteAgentNodeStatus represents agent status on
                        a specific node
                      properties:
                        activeConnections:
                          description: ActiveConnections is the number of active connections
                            on this node
                          format: int32
                          type: integer
                        ip:
                          description: IP is the node's IP address
                          type: string
                        lastSeen:
                          description: LastSeen is when the agent was last seen
                          format: date-time
                          type: string
                        name:
                          description: Name is the node name
                          type: string
                        ready:
                          description: Ready indicates if the agent is ready
                          type: boolean
                        version:
                          description: Version is the agent version on this node
                          type: string
                        vips:
                          description: VIPs are the VIPs assigned to this node
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  ready:
                    description: Ready is the number of ready agents
                    format: int32
                    type: integer
                  total:
                    description: Total is the total number of expected agents
                    format: int32
                    type: integer
                  unhealthy:
                    description: Unhealthy is the number of unhealthy agents
                    format: int32
                    type: integer
                type: object
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connection:
                description: Connection is the current connection status
                properties:
                  activeConnections:
                    description: ActiveConnections is the number of active agent connections
                    format: int32
                    type: integer
                  connected:
                    description: Connected indicates if there are active connections
                      from the remote cluster
                    type: boolean
                  error:
                    description: Error is the last connection error message
                    type: string
                  lastConnected:
                    description: LastConnected is when the last connection was established
                    format: date-time
                    type: string
                  lastDisconnected:
                    description: LastDisconnected is when the last disconnection occurred
                    format: date-time
                    type: string
                  latency:
                    description: Latency is the average round-trip latency to the
                      remote cluster
                    type: string
                type: object
              lastConfigSync:
                description: LastConfigSync is the timestamp of the last successful
                  configuration sync
                format: date-time
                type: string
              lastHeartbeat:
                description: LastHeartbeat is the timestamp of the last heartbeat
                  from agents
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation last observed by
                  the controller
                format: int64
                type: integer
              phase:
                description: Phase is the current phase of the remote cluster
                enum:
                - Pending
                - Connecting
                - Connected
                - Degraded
                - Disconnected
                - Failed
                type: string
              version:
                description: Version is the agent version running in the remote cluster
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
